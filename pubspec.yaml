name: my_webrtc_videocall
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.3
  flutter_webrtc: 1.0.0
  
  # Updated Firebase packages with compatible versions
  firebase_core: ^3.15.2
  cloud_firestore: ^5.4.4
  firebase_storage: ^12.4.10
  
  permission_handler: ^12.0.1
  path_provider: ^2.1.1
  share_plus: ^7.2.1
  image_gallery_saver: ^2.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true